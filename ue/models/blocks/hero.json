{
  "definitions": [
    {
      "title": "Hero",
      "id": "hero",
      "plugins": {
        "da": {
          "rows": 1,
          "columns": 1
        }
      }
    }
  ],
  "models": [
    {
      "id": "hero",
      "fields": [
        {
          "component": "text",
          "name": ".hero picture img",
          "label": "Hero Background Image",
          "valueType": "string",
          "required": true,
          "description": "Background image for the hero section. Will be positioned absolutely to cover the entire hero area."
        },
        {
          "component": "text",
          "name": ".hero h1",
          "label": "Hero Heading",
          "valueType": "string",
          "required": true,
          "description": "Main heading text for the hero banner. Displayed with high contrast over the background image."
        },
        {
          "component": "richtext",
          "name": ".hero p",
          "label": "Hero Description",
          "valueType": "string",
          "description": "Optional description or subtitle text for the hero section."
        },
        {
          "component": "number",
          "name": "data-min-height",
          "label": "Minimum Height (px)",
          "valueType": "number",
          "description": "Minimum height for the hero section in pixels. Default is 300px."
        }
      ]
    }
  ],
  "filters": []
}
